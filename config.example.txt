# ETF fund-flow monitor starter config.
# Copy to config.local.txt and keep local secrets in .local_secrets.local.json or env vars.

cache_dir = data/cache
output_dir = outputs
source_name = tushare
calendar_exchange = SSE
etf_market = E
category_map_path = data/local_reference/etf_category_map.csv

# Used to fetch enough history for dashboard ranges and share-change based flow.
lookback_days = 370

# Report thresholds.
max_report_rows = 30
min_amount_for_alert = 100000000
min_abs_flow_for_alert = 50000000
