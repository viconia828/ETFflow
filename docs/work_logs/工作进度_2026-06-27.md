# 工作进度 2026-06-27

## 背景

- 继续按项目约定维护每日工作日志和下一步清单。
- 上午先调整分享用自包含 HTML 页面；随后追加修正货币 ETF 100 元报价、债券 ETF 报价校验和成交额单位口径。

## 今日代码进展

- 调整 `src/etf_flow_monitor/monitor/html_dashboard.py`：
  - “今日 ETF 明细”不再按估算净额绝对值混排。
  - 改为两个榜单：`流入前十` 按 `estimated_net_flow` 从高到低排序，`流出前十` 按 `estimated_net_flow` 从低到高排序。
  - 每个榜单最多展示 10 只 ETF，并保留代码、名称、分类、涨跌幅、成交额和估算净额。
  - 移除页面上的“分类维护”区域；分类维护继续通过 `data/local_reference/etf_category_map.csv` 和分类校验输出来做。
  - 将顶部第一张核心指标卡从“最新”改为“区间累计”，默认显示 `6月累计估算净额`。
  - 为每个区间按钮预埋对应的顶部指标数据，点击 `最近1日 / 1周 / 2周 / 1月 / 3月 / 6月 / 今年来 / 12月` 时同步刷新顶部的区间累计、当前方向和最大去向。
  - 将顶部“当日”指标的说明从 `请求日期估算净额` 改为带日期的文案，例如 `2026-06-25 当日估算净额`，并同步用于区间切换数据属性和走势视图右侧摘要卡。
  - 重定义“当前方向”口径：`最近1日 / 1周 / 2周 / 1月 / 3月` 与前一等长交易日区间比较，展示 `流出转流入 / 流入转流出 / 净流入扩大或缩小 / 净流出扩大或缩小`；`6月 / 今年来 / 12月` 不做增量比较，只展示区间全量净流入或净流出。
  - 将“当前方向”口径写入“明细”页的“口径说明”。
  - 将页面视图从 `走势 / 轮动` 调整为 `走势 / 轮动 / 明细` 三个 tab。
  - 将“今日 ETF 明细”和“口径说明”移入 `明细` tab，避免在走势和轮动视图下重复出现在页面底部。
  - 点击 `明细` 时隐藏区间与指标控制条；切回 `走势 / 轮动` 时恢复显示。
- 调整 dashboard 样式：
  - 两个明细榜单在宽屏并列展示，窄屏自动上下排列。
  - 删除不再使用的分类维护卡片样式。
- 更新 `tests/test_html_dashboard.py`：
  - 断言页面包含 `流入前十` 和 `流出前十`。
  - 断言页面不再包含 `分类维护`。
  - 断言顶部卡片不再显示“最新”，并存在区间切换所需的数据属性。
  - 断言页面出现带日期的当日说明，并且不再出现 `请求日期估算净额`。
  - 断言短周期方向支持 `流出转流入`，长周期方向显示 `区间净流入/区间净流出`，包含方向口径说明。
  - 断言存在 `明细` tab 和 detail panel，且“今日 ETF 明细”“口径说明”各只渲染一次。
- 更新 `README.md`：
  - 将分类维护说明改为本地 CSV/校验输出维护，不放进分享页面。
  - 将可视化监控器说明更新为 `走势 / 轮动 / 明细` 三个视图。
  - 将 BAT 日期输入提示同步为 ASCII 文案：`Trade date YYYYMMDD, blank = default:`。
  - 将 README 重写为面向用户的使用说明，按首次使用、日常运行、输出位置、页面查看、自动发布、代理联网、缓存起始日、分类维护、资金流口径、命令行和常见问题组织内容。
- 修复 `资金流监控.bat`：
  - 将 BAT 主体改为纯 ASCII，避免 Windows `cmd.exe` 在不同代码页下把中文行解析坏。
  - 将 `echo.` 改为 `echo(`，规避 `echo.` 被错误解析为命令的情况。
  - 对候选 Python 路径增加空值防护，避免空/异常环境变量触发 `'""' 不是内部或外部命令`。
- Git 提交与远端：
  - 初始化本地 Git 仓库并设置远端 `git@github.com:viconia828/ETFflow.git`。
  - 调整 `.gitignore`：继续忽略 `data/cache/`、`outputs/`、本地 secret 和临时目录；允许提交 `data/local_reference/etf_category_map.csv` 作为分类维护基准。
  - 已推送初始提交 `cc87559` 到 `origin/main`。
- GitHub Pages 发布：
  - 建立临时发布仓库 `tmp_gh_pages/`，该目录由主仓库 `.gitignore` 的 `tmp*/` 规则排除。
  - 将 `outputs/flow_monitor/20260626/etf_flow_dashboard.html` 复制为 Pages 根目录 `index.html`，作为最新日报。
  - 将 `outputs/flow_monitor/20260625/etf_flow_dashboard.html` 复制为 `reports/20260625/index.html`。
  - 将 `outputs/flow_monitor/20260626/etf_flow_dashboard.html` 复制为 `reports/20260626/index.html`。
  - 新增 `reports/index.html` 作为日期归档入口，并添加 `.nojekyll`。
  - 已推送 `gh-pages` 分支提交 `046d82a` 到 `git@github.com:viconia828/ETFflow.git`。
  - Pages 访问路径按 GitHub Pages 规则为 `https://viconia828.github.io/ETFflow/`、`https://viconia828.github.io/ETFflow/reports/20260625/`、`https://viconia828.github.io/ETFflow/reports/20260626/`。
- 调整 `src/etf_flow_monitor/monitor/flow_metrics.py`：
  - 新增 `flow_price` 资金流估算价格字段。
  - `fund_type=货币型` 且场内报价大于 10 时，使用 `close / 100` 估算资金流，避免货币 ETF 100 元报价把份额变动放大 100 倍。
  - 普通 ETF 和债券 ETF 继续使用场内收盘价本身，不对 100 元以上债券 ETF 做除以 100。
  - 将 Tushare `fund_daily.amount` 从千元口径换算为元后进入最终快照、市场汇总和页面展示。
- 继续调整静态 dashboard 展示：
  - 移除 `金额（亿元）/ 份额（亿份）/ 规模（亿元）` 指标切换按钮，页面统一按金额（亿元）展示。
  - `走势` 页不再在图表右侧重复展示区间累计、当日和当前方向；口径说明移动到走势页下方。
  - `走势` 页调整为左侧收窄图表、右侧固定区间流向列，静态展示 `最近1日 / 1周 / 2周 / 1月 / 3月 / 6月 / 今年来 / 12月` 的净流入/净流出和金额。
  - 默认口径说明移除“本文件为单日静态快照……”这一句。
  - `明细` 页保留 `最近1日 / 1周 / 2周 / 1月 / 3月 / 6月 / 今年来 / 12月` 区间按钮，并按所选区间展示流入前十、流出前十。
  - 明细页成交额、估算净额和涨跌幅均改为区间口径；区间涨跌幅使用逐日 `pct_change` 复合收益，缺失时用区间首末收盘价兜底。
  - 明细页分类列默认只显示主分类 `category`；当 `subcategory` 不同于主分类时，鼠标悬停显示完整 `category / subcategory`。
- 调整本地缓存窗口配置和更新 helper：
  - 在配置中新增 `local_cache_start_date`，默认 `20260101`，表示用户可生成页面的最早可用日期。
  - helper 实际补缓存起点自动取 `local_cache_start_date - 12个月`，并额外包含前一交易日，用于支持 `12月` 区间和份额变化计算。
  - `资金流监控.bat` 第一阶段缓存更新不再跟随用户输入的报表日期，而是每次按本机上海时区日期更新到最新可用市场日。
  - helper 日常检查默认使用本地横截面缓存文件名形成的完整区间做 fast path；完整覆盖时不逐日扫描全量历史。
  - helper 在拉取官方交易日历前新增可视化日志，显示 `cached_tail / required_tail / action`，方便判断日历尾部是否触发自动补拉。
  - 新增 `--full-check` 参数，用于怀疑中间缓存文件缺失时手动做完整逐日检查。
  - 将原示例配置文件改名为正式配置 `config.txt`，并同步 BAT、README 和工具默认参数。
  - 新增 `tools/publish_pages.py`，可将生成的 dashboard 自动发布到 `gh-pages` 分支：根目录 `index.html` 为最新日报，`reports/YYYYMMDD/index.html` 为日期归档。
  - 将自动发布目标仓库加入配置：`pages_repo_url` 控制发布仓库，`pages_branch` 控制 Pages 分支；发布脚本仍保留命令行 `--repo-url` 临时覆盖。
  - 将 `资金流监控.bat` 改为三步流程：更新缓存、生成 dashboard、发布 GitHub Pages；发布失败会在 BAT 窗口提示 WARN，但不影响本地 dashboard 生成结果。
  - 修复 Windows 下 `tmp_pages_publish/.git/objects` 只读或残留导致发布清理失败的问题：发布脚本删除临时目录时会先改可写再重试；如果旧临时目录仍被占用，会自动切换到带 PID 的临时目录继续发布。
  - 修复非交易日输入下发布日期未对齐的问题：当用户输入日期对应的 dashboard 不存在时，发布脚本会自动选择该日期之前最近一个已生成 dashboard，并按实际交易日写入 Pages 归档。
  - 将 BAT 发布失败提示补充为检查网络、GitHub SSH 或临时发布目录权限。
  - 将 `config.txt` 注释改为中文。
  - 增加代理绕过保护：BAT 清理常见代理环境变量并设置 Tushare 忽略代理；`tools/publish_pages.py` 的 Git 子进程也使用清理后的环境并禁用 SSH ProxyCommand/ProxyJump，避免黑洞代理影响数据抓取或发布。
- 做根目录体检和临时目录清理：
  - 保留当前项目仍需使用的根目录文件：`AGENTS.md`、`README.md`、`config.txt`、`requirements.txt`、`.local_secrets.example.json`、`.gitignore`、`资金流监控.bat`。
  - 删除临时测试目录 `tmponz1k8w1/`。
  - 删除已推送的 Pages 临时发布仓库 `tmp_gh_pages/`；后续发布脚本接入前如需发布，可重新生成临时工作目录。
  - 将 README 从 starter 口吻调整为正式项目口吻。
- 调整 `src/etf_flow_monitor/data/schemas.py`：
  - 移除 schema 标准化阶段的 `amount × 1000`，避免缓存读取、拼接、跨境前值填充时反复改变单位。
- 新增 ETF 生命周期核对工具：
  - 新增 `src/etf_flow_monitor/data/lifecycle.py`，支持从公告标题/正文关键词识别 `share_split / share_conversion / transformation / liquidation / merger` 事件。
  - 新增正式本地事件表 `data/local_reference/etf_lifecycle_events.csv` 和公告输入模板 `data/local_reference/etf_announcements.csv`。
  - 新增 `tools/build_etf_lifecycle_table.py`，从本地公告 CSV 提取生命周期事件，并扫描本地 `fund_share` 横截面缓存里的大比例份额跳变，输出匹配/未匹配审计报告到 `outputs/lifecycle_audit/`。
  - 生命周期审计按 `data/local_reference/etf_category_map.csv` 和 `fund_basic` 形成的 ETF universe 过滤份额缓存，避免 Tushare 横截面中的 `.OF` 场外基金代码混入报告。
  - 新增配置项 `lifecycle_events_path` 和 `announcement_file_path`。
  - 生命周期核对先作为本地辅助报告，不进入分享页面，也暂不自动调整资金流公式。
- 新增独立公告更新入口：
  - 新增 `tools/update_etf_announcements.py`，按 ETF universe 从 Tushare 公告接口更新 `data/local_reference/etf_announcements.csv`，并保留已有人工 `event_date/content` 修订。
  - 新增 `ETF公告更新.bat`，手动执行“公告更新 → 生命周期审计”，不接入日常 `资金流监控.bat` 的缓存 helper。
  - 新增配置项 `announcement_api_name = anns_d`，后续如 Tushare 账号或接口名不同可直接改配置。
  - 新增配置项 `lifecycle_min_share_change_pct = 0.50`，BAT 不再每次询问份额跳变阈值。
  - 将 Tushare “没有接口访问权限”识别为权限错误；公告更新遇到权限错误时立即停止远端请求，改用本地公告 CSV 继续生命周期审计。
  - 复查 Tushare 权限页：公开基金数据权限列表包含 `fund_basic/fund_nav/fund_daily/fund_div/fund_portfolio/fund_adj` 等，未看到基金公告接口；后续真实公告源优先评估上交所/深交所官网基金公告/ETF公告页面。
- 将公告下载主源切换为交易所：
  - 新增 `src/etf_flow_monitor/data/exchange_announcements.py`，用官方页面同款请求头抓上交所和深交所公告。
  - 上交所接口确认：`https://query.sse.com.cn/commonQuery.do`，`sqlId=COMMON_PL_JJXX_JJGG_NEW_L`，返回 `SECURITY_CODE / SSEDATE / TITLE / URL`，PDF 拼 `https://www.sse.com.cn`。
  - 深交所接口确认：`POST https://www.szse.cn/api/disc/announcement/annList`，JSON body 使用 `seDate / stock / channelCode / pageSize / pageNum`，ETF 频道为 `etfNotice_disc`，PDF 拼 `https://disc.static.szse.cn/download{attachPath}`。
  - `config.txt` 新增 `announcement_source = exchange`、`lifecycle_status_path`、`lifecycle_request_plan_path`、`lifecycle_announcement_window_days`。
  - `tools/build_etf_lifecycle_table.py` 新增生命周期状态文件和待抓公告请求清单；仅当未匹配份额跳变存在时生成交易所公告请求窗口。
  - `tools/update_etf_announcements.py` 默认走 `exchange`，读取请求清单后按基金和日期窗口抓公告；Tushare 分支保留为手动备用。
  - `ETF公告更新.bat` 调整为三步：生成生命周期请求清单、按清单抓交易所公告、重建生命周期审计。
  - BAT 代理绕过名单新增 `query.sse.com.cn / www.sse.com.cn / www.szse.cn / disc.static.szse.cn`。
  - README 同步更新为交易所主源、审计状态、待请求清单和公告窗口说明。
- 完善生命周期公告窗口和人工确认回路：
  - `lifecycle_announcement_window_days` 改为配置驱动的官方交易日窗口，当前按未匹配份额跳变前后各 5 个交易日抓取交易所公告。
  - `tools/build_etf_lifecycle_table.py` 生成请求清单时读取本地官方交易日历，不再使用自然日窗口。
  - 新增 `lifecycle_pending_confirmations_path` 和 `lifecycle_manual_confirmations_path`，用于记录“窗口内未抓到公告”的待人工确认项和已人工确认白名单。
  - 新增 `tools/review_lifecycle_pending_confirmations.py`，`ETF公告更新.bat` 启动时先询问上一轮待确认跳变；输入 `Y` 或序号后写入人工确认表，后续审计跳过这些已确认跳变。
  - `tools/update_etf_announcements.py` 抓完交易所公告后，会按请求清单逐条检查窗口内是否有公告；无公告的跳变写入待确认 CSV，抓取失败的代码保留给下一次重试。
  - 生命周期状态只在请求清单为空时推进；已匹配生命周期事件和已人工确认项都视为已处理。
- 加固用户维护 CSV 的 Excel 兼容性：
  - 新增 `read_user_csv / write_user_csv / clean_excel_text / parse_excel_friendly_date`，用户会手工打开的 CSV 统一按 UTF-8 BOM 读写。
  - 支持 Excel 保存后的日期形态：`20260105`、`2026/1/5`、`20260105.0`、`="20260105"`。
  - `data/local_reference/etf_category_map.csv` 的读取、生成、申万细化和校验都切换到用户 CSV helper，保护中文分类和 `="510300.SH"` 这类文本代码。
  - 公告原始表、生命周期事件表、请求清单、待确认表、人工确认表也统一使用用户 CSV helper；程序缓存 CSV 保持原有高性能数值读写路径。
- 调整分类规则和本地分类表：
  - `货币` 分类仅由 `fund_type=货币型` 等更明确条件触发，不再因为名称/指数中出现“现金”就归入货币。
  - 将股票型“自由现金流 ETF”归入 `红利价值 / 自由现金流`。
  - 将宽基识别提前到金融/证券关键词之前，避免 `华夏上证50ETF`、`中银证券中证500ETF` 这类宽基因“证券”字样误归入证券行业。
  - 申万行业细化时剔除 `上海证券交易所 / 深圳证券交易所` 这类交易所名称噪音。
  - 已明确识别的宽基指数、红利价值、商品等分类不再被申万行业覆盖；真正的证券行业 ETF 仍可映射到 `非银金融 / 证券`。
- 更新测试：
  - 新增 `511880.SH` 样本，确认 2026-06-25 货币 ETF `flow_price=1.00545`，估算流出约 `12.39` 亿元。
  - 新增 `511360.SH` 样本，确认债券 ETF `flow_price=113.586`，不做除以 100。
  - 增加横截面缓存读取 `amount` 不被二次放大的断言。
  - 新增宽基优先测试，确认 `华夏上证50ETF` 和 `中银证券中证500ETF` 不会因“证券”字样被误分为证券行业。

## 输出与验证

- 运行时确认：
  - Python: `C:\Users\Ai\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`
  - pandas: `3.0.1`
- 编译验证：
  - `python -X utf8 -m compileall src tools tests` 通过。
- BAT 启动验证：
  - 交互启动 `资金流监控.bat` 后能正常显示 `Trade date YYYYMMDD, blank = default:`。
  - 未再出现 `'""'`、`'EXE'`、`'cho.'` 等批处理解析错误。
  - 在当前沙箱网络受限场景下，缓存 helper 报远端连接失败后按预期降级，并用本地缓存生成 `outputs/flow_monitor/20260625/etf_flow_dashboard.html`。
- HTML 单测：
  - 手动调用 `tests/test_html_dashboard.py::test_write_dashboard_html_is_self_contained` 通过。
  - 新增 `电子 / 半导体材料` 样本后，确认明细表格只显示 `电子`，完整分类只出现在 `title` 悬停文本中。
- 分类显示调整后重新渲染本地页面：
  - `outputs/flow_monitor/20260625/etf_flow_dashboard.html`
  - `outputs/flow_monitor/20260626/etf_flow_dashboard.html`
  - 抽样确认 `title="电子 / 半导体材料">电子</td>` 存在，且未出现可见文本 `>电子 / 半导体材料<`。
- 发布脚本临时目录清理验证：
  - 手动复现并清理残留 `tmp_pages_publish`。
  - 新增只读文件删除测试，覆盖 Windows Git object 清理场景。
  - `tools/publish_pages.py --config config.txt --trade-date 20260626 --dry-run` 通过。
- 非交易日发布日期对齐验证：
  - 新增测试覆盖请求 `20260619` 时自动选择已生成的 `20260618` dashboard。
  - `tools/publish_pages.py --config config.txt --trade-date 20260619 --dry-run` 输出 `requested_trade_date=20260619 resolved_trade_date=20260618`。
- 交易日历尾部可视化验证：
  - 新增测试覆盖 `calendar auto-refresh` 日志背后的状态计算，确认日历覆盖时 `action=cached`、尾部不足时 `action=fetch`。
- 生命周期核对测试：
  - 新增公告关键词识别测试，确认“基金份额折算”公告能生成 `share_conversion` 事件。
  - 新增份额跳变匹配测试，确认 100% 份额跳变可匹配附近 `share_split` 事件。
  - 新增缓存读取过滤测试，确认生命周期审计只保留 ETF universe 内代码。
  - 新增公告更新归一化/合并测试，确认远端公告增量不会覆盖已有人工生效日和备注内容。
  - 新增 Tushare 权限错误分类测试，确认“没有接口访问权限”不会被当成频率限制重试。
- 交易所公告源验证：
  - 轻量探针确认上交所最新基金公告小分页返回 PDF 相对路径。
  - 轻量探针确认深交所 ETF `tannInfo` 和 `annList` 返回 `title/publishTime/attachPath/secCode/secName` 等字段。
  - 用交易所源脚本探针抓 `159290.SZ` 和 `501065.SH` 的 2026-06-27 公告，写入 `outputs/tmp_exchange_announcements_probe.csv`，返回 `fresh_rows=5`、`errors=0`。
  - 临时 CSV 字段为 `fund_code,announcement_date,event_date,title,content,source_url`，可直接进入生命周期事件提取流程。
  - 新增交易所公告归一化、请求窗口合并、交易所抓取循环和公告请求清单测试。
- 巨潮公告源验证：
  - `python -X utf8 -m compileall src tools tests` 通过。
  - 手动调用巨潮归一化和待确认生成两个生命周期测试函数通过。
  - 用新 `CninfoAnnouncementClient` 抓取 `159211.SZ` 在 `2025-07-03` 至 `2025-07-08` 的公告窗口，返回 `fresh_rows=1`、`errors=0`，探针 CSV 中公告日期为 `20250704`。
  - 新增 `tools/probe_cninfo_announcement_rate.py`，用于对比巨潮公告请求间隔。
  - 以高疑似请求清单前 21 个合并窗口测试：`0.5s` 耗时 `13.92s`、`0.2s` 耗时 `8.01s`、`0s` 耗时 `3.66s`，三档均 `errors=0`。
  - 扩大到 81 个合并窗口测试：`0.2s` 耗时 `27.96s`、`0s` 耗时 `11.47s`，两档均 `errors=0`。
  - 正式默认不设为无间隔，新增 `announcement_sleep_seconds = 0.20` 放入配置，BAT 默认按配置使用。
  - 当前 bundled Python 未安装 `pytest`，本轮未跑完整 pytest。
- 生命周期审计工具本地试跑：
  - 命令：`tools/build_etf_lifecycle_table.py --config config.txt --min-share-change-pct 0.50`。
  - 输出：`outputs/lifecycle_audit/etf_lifecycle_share_jump_audit_20260627_130325.*`。
  - 当前公告模板为空，因此 `event_rows=0`，待填真实公告后自动匹配。
  - ETF universe 过滤后，扫描份额缓存 `688663` 行、监控代码 `1582` 只，识别大比例份额跳变 `871` 条。
- 生命周期人工确认验证：
  - 新增测试覆盖官方交易日窗口、人工确认跳过请求计划、未抓到公告生成待确认表、Excel 友好日期解析和分类 map 中文读取。
  - 相关测试：`python -X utf8 -m pytest -o pythonpath=src tests\test_lifecycle.py tests\test_category_map.py tests\test_config_and_cache_window.py`，31 条通过。
  - 用临时目录 smoke 验证 `tools/review_lifecycle_pending_confirmations.py --yes` 可把待确认跳变写入人工确认表，并清空待确认清单。
- 本地审计跳变小样本公告回填验证：
  - 先重建本地生命周期请求清单，得到 `request_plan_rows=871`。
  - 从本地份额跳变中选取两批样本窗口，使用交易所公告源抓取，上交所/深交所合计写入公告表 `50` 条。
  - 第二批命中 6 条“基金份额拆分结果”公告，解析回填 `data/local_reference/etf_lifecycle_events.csv`。
  - 重建审计后 `event_rows=6`、`event_type_counts={"share_split": 6}`、`matched_share_jump_rows=6`、`request_plan_rows=865`。
  - 精确校验 6 个已匹配跳变键均已从 `data/local_reference/etf_lifecycle_announcement_requests.csv` 中移除；同一基金其他日期跳变仍保留待核对。
  - 首批样本中的简称变更、溢价风险、停牌、最小申赎单位调整等公告未被误识别为生命周期事件，说明当前标题关键词解析偏保守。
- 生命周期跳变审计范围画像：
  - 新增 `tools/profile_lifecycle_jump_scope.py`，基于本地审计 CSV 和分类表输出跳变范围画像。
  - 输出文件：
    - `outputs/lifecycle_audit/lifecycle_jump_scope_profile_20260627.csv`
    - `outputs/lifecycle_audit/lifecycle_jump_scope_summary_20260627.md`
  - 当前全量跳变 `871` 条，其中 `50%-100%` 变化占 `556` 条，说明 `0.50` 单阈值过粗。
  - 货币 ETF 跳变 `53` 条，上市 60 日内跳变 `117` 条，均是高噪声来源。
  - 若使用“正向整数倍跳变或 50% 以上负向跳变，排除货币 ETF 和上市 60 日内样本”作为高疑似层，待抓范围可由 `871` 缩至 `126` 条，并保留当前已验证的 6 条拆分样本。
  - 若担心非整数比例折算漏掉，可在高疑似层基础上加入“正向变化 >=200%”，范围为 `188` 条，仍大幅低于当前全量。
- 生命周期请求清单正式改为分层输出：
  - 新增配置项 `lifecycle_observation_plan_path`、`lifecycle_high_suspicion_min_listing_days`、`lifecycle_integer_ratio_tolerance`、`lifecycle_high_suspicion_positive_min_pct`、`lifecycle_high_suspicion_negative_max_pct`。
  - `data/local_reference/etf_lifecycle_announcement_requests.csv` 现在只写高疑似自动抓取层。
  - `data/local_reference/etf_lifecycle_observation_jumps.csv` 写低疑似观察层，不再喂给交易所公告抓取。
  - 当前正式规则为：排除货币 ETF、排除上市 60 日内样本，保留正向整数倍跳变、正向变化 >=200%、负向变化 <=-50%。
  - 用本地缓存重建后，高疑似待抓 `182` 条，低疑似观察 `683` 条；已匹配的 6 条拆分事件不再进入两张未处理清单。
- 公告更新 BAT 输出收敛：
  - `tools/build_etf_lifecycle_table.py` 和 `tools/update_etf_announcements.py` 新增 `--verbose`，默认 BAT 窗口只显示关键数量、报告路径和必要提醒。
  - 完整 JSON 仍写入 `outputs/lifecycle_audit/`，需要排查时可手动加 `--verbose` 打印到控制台。
- 交易所公告抓取稳健性调整：
  - 将交易所公告请求间隔默认调为 1 秒，并在每个基金窗口请求后也实际等待，避免单页请求绕过限速。
  - 深交所/上交所抓取遇到连接被远端断开时默认重试 3 次，失败后记入本轮摘要并继续抓后续窗口，不再默认因 10 个错误直接丢掉本轮已抓结果。
  - 新增 `tools/print_lifecycle_run_summary.py`，`ETF公告更新.bat` 结束前汇总公告抓取任务、失败任务、本次公告数、生命周期已审计跳变和高疑似待抓数量。
- 公告主源切换为巨潮资讯：
  - 验证巨潮 `fund_stock.json` 可提供基金 `code/orgId` 映射，`hisAnnouncement/query` 使用 `column=fund`、`tabName=fulltext`、`stock=代码,orgId` 可查询基金公告。
  - 用 `159211.SZ` 在 `2025-07-03` 至 `2025-07-08` 窗口验证巨潮返回对应公告和 PDF 路径。
  - 新增 `src/etf_flow_monitor/data/cninfo_announcements.py`，公告更新默认源改为 `cninfo`，上交所/深交所官网源保留为 `exchange` 备用。
  - `ETF公告更新.bat` 不再硬编码交易所源，改为读取 `config.txt` 的 `announcement_source`；代理绕过名单新增巨潮域名。
  - README、`config.txt` 和待人工确认提示同步改为巨潮/公告窗口口径。
- 使用本地缓存重新生成 2026-06-25 页面：
  - `outputs/flow_monitor/20260625/etf_flow_dashboard.html`
  - 生成文件包含 `流入前十` 和 `流出前十`。
  - 生成文件不包含 `分类维护`。
  - 实际页面中 `流入前十` 10 行、`流出前十` 10 行。
  - 顶部第一张卡默认显示 `6月累计估算净额`，8 个区间按钮均包含各自的 `data-period-net-text`。
  - 顶部“当日”卡和 8 个区间按钮的 `data-daily-caption` 均显示 `2026-06-25 当日估算净额`。
  - 实际页面中 `1日 / 1周 / 2周 / 1月 / 3月` 使用上一等长区间比较；`6月 / 今年来 / 12月` 显示 `全量方向`。
  - “明细”页“口径说明”包含方向口径定义。
  - 生成文件包含 `data-view-target="detail"` 和 `data-view-panel="detail"`。
  - 生成文件中 `今日 ETF 明细` 和 `口径说明` 均只出现 1 次。
  - HTML 单测新增 `电子 / 半导体材料` 样本，确认明细单元格只显示 `电子`，完整分类仅放在 hover 标题中。
- 手动调用 17 个轻量测试函数通过，包括资金流、横截面缓存、HTML、分类、交易日历和分类表校验相关测试。
- 分类表校验通过：
  - `outputs/category_map_validation/category_map_validation_20260627_005539.json`
  - `rows=1582`
  - `blocking_issues=0`
  - `pending_review=0`
- 重新生成 2026-06-25 页面后抽样核对：
  - `511880.SH` 银华货币 ETF-A：`close=100.545`，`flow_price=1.00545`，`amount=17,248,103,242` 元，`estimated_net_flow=-1,239,247,288.5` 元，页面显示约 `-12.39` 亿元。
  - `511360.SH` 海富通中证短融 ETF：`close=113.586`，`flow_price=113.586`，未除以 100。
  - `511010.SH` 国泰上证 5 年期国债 ETF：`close=140.636`，`flow_price=140.636`，未除以 100。
- 重新细化并校验分类表：
  - `tools/refine_category_map_with_sw.py --config config.txt` 成功，`refined_rows=289`。
  - `outputs/category_map_validation/category_map_validation_20260627_010333.json`
  - `rows=1582`
  - `blocking_issues=0`
  - `pending_review=0`
- 重新生成 2026-06-25 页面后抽样核对分类：
  - `510050.SH` 华夏上证50ETF：`宽基 / 上证50`。
  - `510680.SH` 万家上证50ETF：`宽基 / 上证50`。
  - `515190.SH` 中银证券中证500ETF：`宽基 / 中证500`。
  - `510880.SH` 华泰柏瑞上证红利ETF：`红利价值 / 红利价值`。
  - `510200.SH` 汇安上证证券ETF：`非银金融 / 证券Ⅱ`。
  - `512000.SH` 华宝中证全指证券公司ETF：`非银金融 / 证券Ⅲ`。
- 生命周期公告复查和资金流修正接入：
  - 清盘关键词收紧为严格事件：`终止上市`、`进入清算`、`清算报告`、`基金合同终止并清算`；“可能触发基金合同终止情形”只作为向后探测线索，不直接写入生命周期事件。
  - 季度报告、流动性服务商、净值低于 5000 万提示、溢价风险提示等公告自动确认为无资金流生命周期事件。
  - 将完全无公告窗口的二轮复查窗口放入配置：`lifecycle_no_announcement_retry_window_days = 10`。
  - 巨潮正式重抓扩窗请求：主窗口 `105/105` 成功，新增公告 `41` 条；清盘风险提示触发向后探测 `1` 个任务，新增公告 `30` 条，失败 `0`。
  - 二轮复查后仍无严格生命周期事件的跳变自动写入 `data/local_reference/etf_lifecycle_manual_confirmations.csv`，不再要求 BAT 黑窗口人工确认。
  - 最终生命周期审计：`share_jump_rows=871`、`event_rows=19`、`matched_share_jump_rows=19`、`manual_confirmed_share_jump_rows=170`、`request_plan_rows=0`、`observation_plan_rows=682`，状态已核对至 `20260626`。
  - 生成资金流修正表 `data/local_reference/etf_lifecycle_flow_adjustments.csv`，当前 `19` 条，均为份额拆分事件。
  - 日常报表生成已接入修正表：本地生成 `20260626` 页面时读取修正表 `19` 条，实际修正报告窗口内 `18` 条，修正估算净额合计约 `-425.26` 亿元。
  - 修复 `--skip-if-current` 分支误清空修正表的问题，跳过审计时保留既有 `flow_adjustments`。
  - `tools/print_lifecycle_run_summary.py` 改为读取当前 pending 文件，并优先展示最近一次非空公告抓取摘要，避免最终 BAT 摘要误报旧 pending 数。
  - 验证命令：
    - `python -m compileall src tools tests`
    - 生命周期 smoke assertions 通过。
    - `tools/build_etf_lifecycle_table.py --config config.txt --force`
    - `tools/update_etf_announcements.py --config config.txt`
    - `python -m etf_flow_monitor.cli --config config.txt --trade-date 20260626 --cache-only`
- GitHub Pages 归档排序修复：
  - `tools/publish_pages.py` 发布归档时继续按 `reports/YYYYMMDD` 生成目录。
  - `reports/index.html` 明确按报告日期倒序排列。
  - 根目录 `index.html` 不再简单使用“本次发布”的页面，而是重新扫描归档目录后复制报告日期最新的一期，避免补发旧日期报告时首页被旧日报覆盖。
  - 新增发布排序 smoke test：先发布 `20260626`，再补发 `20260618`，确认首页仍为 `20260626`，归档入口中 `2026-06-26` 位于 `2026-06-18` 前。
- GitHub Pages 旧报告缓存修复：
  - 复查远端 `origin/gh-pages:reports/20260601/index.html`，确认分支内容已更新，但浏览器/Pages 缓存仍可能显示旧生成时间。
  - `reports/index.html` 的报告链接改为 `./YYYYMMDD/?v=<html内容hash>`，同一报告日期重发后 URL 指纹变化，避免普通刷新命中旧缓存。
  - dashboard HTML `<head>` 增加 `Cache-Control / Pragma / Expires` no-cache meta，降低后续静态页面被浏览器缓存的概率。
  - 发布脚本打印的 Pages URL 也带 `?v=<hash>`，BAT 窗口复制链接即可打开新版本。
  - 已重新生成并发布 `20260601`，远端分支确认生成时间为 `2026-06-27 16:35:49`，归档入口链接为 `./20260601/?v=ef4d461381e9`。
- 复核 2026-06-25 完整跑数中 `fund_daily` 缺失 ETF：
  - 原始缺口 `14` 只，其中 `8` 只有明确 `list_date > 2026-06-25`，属于报表日之后上市/待上市样本。
  - `src/etf_flow_monitor/cli.py` 新增缺行情后的未来上市归因：只有当代码没有返回 `fund_daily` 时，才读取 `fund_basic` 和原始分类表里的 `list_date`；若 `list_date` 晚于报表日，则从 `fund_daily_missing_funds` 中跳过。
  - run ledger 保留 `fund_daily_missing_funds_raw`、`fund_daily_future_listed_funds` 和 `fund_daily_future_listed_codes`，方便后续审计原始缺口。
  - 重新生成 2026-06-25 报表后，最新 ledger 为 `outputs/logs/flow_monitor_20260627_212048/run.json`：`fund_daily_missing_funds_raw=14`、`fund_daily_future_listed_funds=8`、`fund_daily_missing_funds=6`。
  - 剩余 `6` 只缺行情 ETF 的 `list_date` 仍为空，人工复看均为近期进入静态 universe 的样本，可能尚未到场内上市交易时间；暂不补 `list_date`，后续随每日 `fund_daily` 横截面缓存更新继续观察。
  - 新增测试覆盖“只在缺行情后跳过未来上市代码；已经有行情的代码即使元数据日期异常也不被过滤”。
- 验证 Tushare `fund_share.fd_share` 总份额口径：
  - 从本地 `fund_share` 横截面抽样宽基、港股、货币、债券、商品、半导体 ETF，`fd_share × 10,000` 的总份额量级与外部核对一致。
  - 外部核对修正样本：`159915.SZ` 易方达创业板 ETF 约 `110.29` 亿份，`159792.SZ` 富国中证港股通互联网 ETF 约 `637.83` 亿份；其他样本四舍五入到小数点后两位一致。
  - 结论：`fund_share.fd_share` 可按 ETF 总份额的“万份”口径用于 `share_change × 10,000 × flow_price`。
  - README 资金流口径同步改为已抽样核对总份额口径。
- 验证 Tushare `fund_daily.vol` 单位和页面展示策略：
  - 用 2026-06-26 本地 `fund_daily` 横截面抽样 `510300.SH`、`510050.SH`、`159915.SZ`、`159792.SZ`、`511880.SH`、`511360.SH`、`518880.SH`、`561980.SH`、`588200.SH`。
  - 按 `amount × 1000 / (vol × 100)` 反推成交均价，9 个样本均落在当日 `low/high` 区间内，说明 `fund_daily.vol` 按“手”口径自洽。
  - 页面展示策略确认：当前继续以成交额和估算净额为准，不展示成交量、规模或份额视图；`volume` 原始字段保留在 CSV 明细中。
  - README 资金流口径同步记录 `fund_daily.vol` 抽样核对为“手”口径。
- 调整 dashboard `走势` 页布局：
  - 左侧图表列收窄，右侧新增固定区间流向列，展示 `最近1日 / 1周 / 2周 / 1月 / 3月 / 6月 / 今年来 / 12月` 的净流入/净流出方向和亿元金额。
  - 右侧列在桌面宽度下保持 `sticky`，窄屏下随图表上下排列。
  - 重新生成 `outputs/flow_monitor/20260625/etf_flow_dashboard.html`，静态校验确认 `trend-summary-row=8` 且 8 个区间标签均存在；内置浏览器桌面布局实测为左侧图表约 `882px`、右侧列 `310px`，两列无重叠。
  - 继续微调走势页：右侧区间流向外框与左侧图表外框上下对齐；左侧趋势图纵轴标签从绘图区右侧移到左侧。
  - 重新验证 active `6M` 面板：左图与右侧区间流向卡 `topDelta=0`、`bottomDelta=0`，纵轴标签位于绘图区左侧并保留约 `13px` 间距。
  - 将 dashboard 中“标题 + 注释”样式统一为 `section-caption`：注释行统一 `14px`、字重 `900`、上间距 `8px`、下间距 `18px`，走势页、轮动页、明细页和右侧区间流向列一致。
  - 浏览器 computed style 复核：走势页 `金额单位：亿元，负值代表资金净流出` 与右侧 `单位：亿元` 的字号、字重和上下间距完全一致；轮动统计小标签未被新样式影响。
- 完成 P2 生成速度评估并落地区间批量生成：
  - 新增 `tools/build_flow_reports_range.py`，按输入自然日期区间取官方交易日，一次读取区间所需的完整历史横截面缓存，在内存中切片生成每个交易日报告，避免逐日 CLI 重复读取相同 12 个月窗口。
  - `TushareEtfSource.get_etf_daily_by_trade_dates/get_etf_share_by_trade_dates` 新增 `columns` 参数；单日报表和区间批量默认只读取报表生成需要的字段：daily 读取 `fund_code/trade_date/close/pct_change/volume/amount`，share 读取 `fund_code/trade_date/shares/source`。
  - 区间模式默认 `detail_output=today`：dashboard 和 markdown 仍使用完整 12 个月历史窗口，`etf_flow_snapshot.csv` 仅写报表日当天明细，减少批量生成时重复写大 CSV。
  - 区间脚本启动时打印 P2 preflight：朴素逐日读取横截面次数、批量复用后的读取次数、预计节约次数、按需字段和明细输出模式。
  - 区间脚本运行时输出 `[range heartbeat]` 心跳，显示当前阶段、已完成数量、当前日期和耗时；同时写 `outputs/logs/flow_monitor_range_*/run.json`。
  - 补充 2/3 预处理阶段心跳：读取 daily/share 横截面缓存时按交易日更新 `load_fund_daily/load_fund_share` 进度；跨境前值填充、生命周期修正、分类合并和市场汇总预计算阶段也按 `build_flow_snapshot/apply_lifecycle_adjustments/apply_category_map/build_market_summary` 等阶段报心跳。
  - 修复官方日历缓存重复行导致同一交易日重复生成的问题，区间交易日列表会稳定去重。
- 改造 `资金流监控.bat`：
  - 新增模式菜单：回车默认 `1`，保持原有单日报表流程；输入 `2` 后要求输入起止日期，生成区间内所有交易日报表。
  - 区间模式先按区间结束日更新缓存，再调用区间批量脚本，最后调用 Pages 发布脚本一次性发布区间内已生成 dashboard。
- 扩展 `tools/publish_pages.py`：
  - 新增 `--range-start/--range-end`，可一次性 stage 多个 `outputs/flow_monitor/YYYYMMDD/etf_flow_dashboard.html` 到 `gh-pages` 归档目录，并保持首页指向报告日期最新的一期。
  - `--dry-run` 可打印区间内将发布的 dashboard 数量、目标分支和最新报告 URL。
- 验证区间批量 smoke：
  - `tools/build_flow_reports_range.py --config config.txt --start-date 20260625 --end-date 20260626 --cache-only --detail-output today --heartbeat-seconds 5` 通过。
  - P2 preflight 显示 `reports=2`、`source_dates=244`、朴素横截面读取 `972` 次、批量读取 `488` 次、预计节约 `484` 次。
  - `tools/publish_pages.py --config config.txt --range-start 20260625 --range-end 20260626 --dry-run` 通过，识别 dashboard 数量 `2`。
- 修复生命周期公告更新在缓存起点前移后仍误判“无需更新”的问题：
  - 原因确认：`ETF公告更新.bat` 调用 `tools/build_etf_lifecycle_table.py --skip-if-current` 时，旧逻辑只检查 `verified_through >= 最新份额缓存日`，没有检查“已核对起点”；因此 `local_cache_start_date` 前移并刷新出更早份额缓存后，仍可能被旧状态跳过。
  - `tools/build_etf_lifecycle_table.py` 改为读取份额缓存的 `data_earliest_date/data_latest_date`，状态文件新增 `verified_from/data_earliest_date/local_cache_start_date`；旧状态缺少 `verified_from` 时自动失效重建。
  - 用当前 `config.txt` 重新跑 `tools/build_etf_lifecycle_table.py --config config.txt --skip-if-current` 后，缓存区间识别为 `20221230` 至 `20260626`，生成高疑似待抓公告 `70` 条、低疑似观察 `857` 条；新增高疑似全部为 `2023` 年跳变。
  - 第一轮巨潮更新 `jobs=54/completed=54/errors=0`，新增公告 `216` 条；其中 `25` 条跳变因窗口内只有非生命周期公告被自动确认，`2` 条跳变被新增拆分结果公告匹配，剩余 `43` 条进入无公告 pending 并扩到前后 `10` 个官方交易日。
  - 第二轮巨潮更新 `jobs=33/completed=33/errors=0`，新增公告 `5` 条；`43` 条 pending 中 `35` 条扩窗后仍无公告、`8` 条扩窗后有公告但无生命周期关键词，均自动确认为非生命周期事件。
  - 单独复核债券型 `511220.SH` 海富通上证城投债ETF：2023-01-12 至 2023-01-13 份额由 `1443.81` 万份增至 `14438.14` 万份，2023-01-16 `pre_close=9.532` 约等于 2023-01-13 `close=95.318` 的 `1/10`；上交所官方公告接口确认 2023-01-10 存在实施份额拆分公告、2023-01-16 存在拆分结果公告，已写入 `etf_announcements.csv`，并从“无生命周期事件”确认中移除。
  - 重新构建后生命周期事件 `25` 条、已匹配跳变 `22` 条；`data/local_reference/etf_lifecycle_flow_adjustments.csv` 新增 `511220.SH` 的 `zero_estimated_net_flow` 修正，公告日记录为 `20230110`、事件日为 `20230113`，source URL 指向上交所 PDF。
  - 最终状态文件 `data/local_reference/etf_lifecycle_status.json` 标记 `lifecycle_current=true`、`request_plan_rows=0`、`verified_from=20221230`、`verified_through=20260626`；另一台机器拉取 reference CSV/JSON 后，重建落在该区间内的缓存不会重复查证生命周期公告。

## 尚未完成

- 人工复核 `data/local_reference/etf_category_map.csv`，尤其是 `宽基` 中的主题 ETF。
- 继续观察低疑似层 `data/local_reference/etf_lifecycle_observation_jumps.csv` 中的普通申赎噪声，不接入分享页面。
