# 工作进度 2026-07-19

## 159327.SZ 定向修复

- 问题根因不是净值单位，而是 2026-07-17 的份额拆分被当成申购：份额从 `70,152.4697` 万份变为 `198,457.4091` 万份，原始代理按 `份额变化 × 10,000 × flow_price` 计算，即 `128,304.9394 × 10,000 × 3.348 = 4,295,649,371.112` 元。
- 定向补抓 `159327.SZ` 的官方公告，补入 2026-07-14 拆分实施公告和 2026-07-18 拆分结果公告。
- 生命周期审计已将 2026-07-17 跳变匹配为 `share_split`，资金流动作是 `zero_estimated_net_flow`。
- 重建 2026-07-17 产物后，快照保留原始代理值 `4,295,649,371.112` 元及等额负向调整，最终估算净额为 `0`，方向为 `flat`，不再进入流入榜。
- 2026-07-17 全市场估算净流入更新为 `65,345,459,629.70` 元。同日共有 4 只已确认拆分 ETF 的代理流入归零，因此总额变化不只来自 `159327.SZ`。

## low_suspicion_observation 全量复审

- 以原 observation 表执行定向公告复审：754 个请求任务、436 只基金；新增抓取 2,680 条公告，本地公告表从 417 条增至 2,862 条。
- 首轮全量复审后，生命周期已匹配跳变从 61 条增至 110 条，原低疑似中发现 49 条类似漏抓；low observation 从 879 条降至 830 条。
- 49 条新增匹配中包括近似整数倍但伴随同期申赎的拆分，以及上市初期的份额折算。旧正向 `>=200%` 条件只能覆盖其中 14 条，不能继续作为主要边界。
- 官方交易所兜底在全量复审时有 80 个窗口失败，主要为深交所端主动断连；失败窗口保留为显式未完成项，没有自动确认为无生命周期事件。
- 复审产物：`outputs/lifecycle_audit/etf_lifecycle_share_jump_audit_20260719_115258.csv`、`outputs/lifecycle_audit/etf_lifecycle_share_jump_audit_20260719_115258.md`、`outputs/lifecycle_audit/announcement_update_20260719_115240.json`。

## 高疑似规则修改

- `lifecycle_integer_ratio_tolerance`：`0.03 -> 0.20`，覆盖 `159327.SZ` 的 `2.828944` 倍份额比及拆分叠加申赎的情形。
- `lifecycle_high_suspicion_positive_min_pct`：`2.00 -> 1.20`。
- 新增 `lifecycle_high_suspicion_early_listing_abs_min_pct = 0.70`：上市未满 60 日不再一律降为低疑似，绝对份额跳变达到 70% 时进入高疑似。
- 仍排除货币 ETF；常规负向阈值仍为 `-0.50`。
- 用 49 条新增生命周期匹配回放，新组合能覆盖全部 49 条；改规则后先用本地公告自动确认 254 条非生命周期窗口，避免无差别扩大远端请求。
- 两轮新增阈值补抓后，生命周期事件 197 条、已匹配 112 条、人工确认非事件 514 条、low observation 492 条、高疑似待抓 38 条。
- 第二轮扩大窗口自动确认 39 条无生命周期事件；仍有 32 个深市代码的交易所接口连续两次断连，以及 2 条首次无公告 pending。状态保持 `lifecycle_current = false`，未把失败窗口误写成“无事件”。
- 最终审计产物：`outputs/lifecycle_audit/etf_lifecycle_share_jump_audit_20260719_121015.csv`、`outputs/lifecycle_audit/etf_lifecycle_share_jump_audit_20260719_121015.md`、`outputs/lifecycle_audit/announcement_update_20260719_120953.json`。

## 验证

- 项目运行时缺少 `pytest`，系统 Python 虽有 pytest 但缺少 pandas；遵循运行时策略，未安装第二套依赖。
- 使用项目运行时直接执行 33 个无 fixture 回归测试，另执行 1 个配置临时路径测试，合计 34 个测试函数全部通过；覆盖公告窗口、高低疑似分层、配置默认值与覆盖等路径。
- 全量生命周期构建成功；2026-07-17 缓存报告重建成功，运行账本为 `outputs/logs/flow_monitor_range_20260719_121055/run.json`，状态 `success`。
- `git diff --check` 未发现空白错误，仅提示 Windows 行尾转换。
- 本次未修改缓存 schema；资金流原始公式未变，修改的是生命周期识别阈值与已确认事件的归零调整。

## 修复影响区间重生成与发布

- 当前公开报告从 2024-09-02 开始，dashboard 包含滚动 12 个月历史。由于本次新增调整最早会进入 2024-09-02 的历史窗口，因此重生成完整公开区间 `20240902-20260717`，而不是只生成生命周期事件发生日。
- 使用本地官方交易日历和缓存批量生成 452 个交易日报告，源数据窗口为 `2023-09-01..2026-07-17`，未发起远端行情抓取。
- 批量运行成功，账本：`outputs/logs/flow_monitor_range_20260719_122041/run.json`；`generated_reports = 452`、`status = success`。
- 发布前核验本次相对仓库基线新增的 52 条公开区间生命周期调整，覆盖 30 个事件日：对应快照最终净额全部为 0，动作全部为 `zero_estimated_net_flow`；`159327.SZ` 未出现在 2026-07-17 流入榜。
- 38 条高疑似请求和 2 条 pending 保持不变，未在本轮生成/发布中重新抓取或自动确认。
- 已将 452 份 dashboard 推送至 `gh-pages`，GitHub Pages deployment 返回 `success`。
- 最新页面：`https://viconia828.github.io/ETFflow/reports/20260717/?v=0d23dfef09fa`；归档页和首页均在线返回 HTTP 200，线上 2026-07-17 页面不含 `159327.SZ` 的原错误流入排名。
