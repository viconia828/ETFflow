# 下一步工作 list 2026-06-27

## 已完成

- [x] 将“今日 ETF 明细”改为 `流入前十` 和 `流出前十` 两个榜单。
- [x] 两个明细榜单分别按净流入、净流出方向排序，并各保留前 10 只 ETF。
- [x] 从分享用 HTML 页面移除“分类维护”区域。
- [x] 将 dashboard 视图切换改为 `走势 / 轮动 / 明细` 三个 tab。
- [x] 将“今日 ETF 明细”和“口径说明”移入 `明细` tab，避免在走势/轮动下重复显示。
- [x] 点击 `明细` 时隐藏区间控制条，切回 `走势 / 轮动` 时恢复。
- [x] 更新 HTML dashboard 测试，防止分类维护区块回归到页面上。
- [x] 重新生成 `outputs/flow_monitor/20260625/etf_flow_dashboard.html` 并验证两个榜单各 10 行。
- [x] 更新 README 中关于分类维护展示位置的说明。
- [x] 将 README 重写成面向普通用户的使用说明。
- [x] 将顶部“最新”指标改为“区间累计”，并明确默认区间为 `6月`。
- [x] 让顶部核心指标随区间按钮切换同步刷新。
- [x] 将顶部“当日”指标说明改为带具体日期的 `YYYY-MM-DD 当日估算净额`。
- [x] 重定义“当前方向”：3 个月以内做上一等长交易日区间比较，3 个月以上只显示区间全量净流入/净流出。
- [x] 将“当前方向”的比较口径写入页面底部备注。
- [x] 修正货币 ETF 100 元报价口径：`fund_type=货币型` 且报价大于 10 时，资金流估算价格使用 `close / 100`。
- [x] 检查债券 ETF 100 元以上报价口径：债券 ETF 继续使用场内收盘价本身，不做除以 100。
- [x] 将 Tushare `fund_daily.amount` 从千元口径换算为元后进入最终快照和页面展示。
- [x] 避免在 schema/cache 标准化阶段反复对 `amount` 乘 1000。
- [x] 修正“自由现金流 ETF”被误归入 `货币` 的分类规则和本地分类表。
- [x] 重新生成 `outputs/flow_monitor/20260625/etf_flow_dashboard.html`，并抽样验证 `511880.SH / 511360.SH / 511010.SH`。
- [x] 运行分类表校验，确认 `blocking_issues=0`、`pending_review=0`。
- [x] 修正宽基 ETF 因“证券交易所”或基金公司名含“证券”被误归入证券行业的问题。
- [x] 重新细化 `data/local_reference/etf_category_map.csv`，确认 `510050.SH` 为 `宽基 / 上证50`，真正证券 ETF 仍保留 `非银金融 / 证券`。
- [x] 重新生成 `outputs/flow_monitor/20260625/etf_flow_dashboard.html`，确认页面明细中 `华夏上证50ETF` 显示为 `宽基 / 上证50`。
- [x] 修复 `资金流监控.bat` 编码/解析问题，将 BAT 主体改为纯 ASCII 并去掉 `echo.`。
- [x] 交互启动验证 BAT 能正常进入日期提示，沙箱网络失败时仍能降级使用本地缓存生成页面。
- [x] 初始化 Git 仓库，设置远端 `git@github.com:viconia828/ETFflow.git`，提交并推送到 `origin/main`。
- [x] 调整 `.gitignore`，提交分类维护表 `data/local_reference/etf_category_map.csv`，继续排除缓存、输出和本地 secret。
- [x] 将已生成的 2026-06-25、2026-06-26 两个 dashboard 复制成 GitHub Pages 结构并推送到 `gh-pages` 分支。
- [x] 移除 dashboard 的 `金额 / 份额 / 规模` 指标切换按钮，统一按金额（亿元）展示。
- [x] 将 `走势` 页下方改为口径说明，不再重复展示区间累计、当日和当前方向。
- [x] 从默认口径说明移除“本文件为单日静态快照……”一句。
- [x] `明细` 页保留全套区间按钮，并按所选区间展示流入前十、流出前十。
- [x] 明细页涨跌幅改为区间涨跌幅。
- [x] 明细页分类列默认只显示主分类，鼠标悬停显示完整 `category / subcategory`。
- [x] 在配置中新增 `local_cache_start_date`，作为用户可用起始日；正式 `config.txt` 保留当前本地使用值。
- [x] 缓存 helper 按用户可用起始日往前 12 个月并额外包含前一交易日来补齐后台缓存。
- [x] BAT 启动时缓存 helper 改为按本机时间更新到最新可用市场日，用户输入日期只控制报表生成。
- [x] 优化缓存 helper 日常检查：完整缓存区间已覆盖时走 fast path，并提供 `--full-check` 做手动完整检查。
- [x] 缓存 helper 增加交易日历尾部可视化日志，显示是否需要自动补拉官方交易日历。
- [x] 将原示例配置文件改名为正式配置 `config.txt`，并同步入口脚本和文档。
- [x] 完成根目录体检，清理临时目录 `tmponz1k8w1/` 和 `tmp_gh_pages/`。
- [x] 增加 GitHub Pages 发布脚本，并接入 `资金流监控.bat` 自动发布流程。
- [x] 将 GitHub Pages 发布目标仓库和分支放入 `config.txt`，支持后续直接改配置切换发布位置。
- [x] 修复 Windows 下 Pages 临时发布目录 `.git/objects` 残留或只读导致的清理失败。
- [x] 修复非交易日生成页面时，发布脚本仍按用户输入自然日查找 dashboard 的问题。
- [x] 发布失败时在 BAT 窗口输出 WARN，保留本地生成结果。
- [x] 将 `config.txt` 注释改为中文。
- [x] 增加代理绕过保护，避免本机黑洞代理影响数据抓取和 Pages 发布。
- [x] 复核 2026-06-25 完整跑数中 14 只缺失 `fund_daily` 的 ETF，其中 8 只确认为报表日之后上市/待上市。
- [x] 增加报表日之后上市 ETF 的缺行情豁免统计：先抓 `fund_daily`，只有缺行情后才按 `list_date > report_date` 判断并跳过。
- [x] 对剩余 6 只 `list_date` 为空且仍无 `fund_daily` 的近期样本，暂不补日期，后续随每日行情缓存更新继续观察。
- [x] 验证 Tushare `fund_share/fd_share` 可按 ETF 总份额的“万份”口径使用，并同步 README 资金流说明。
- [x] 验证 Tushare `fund_daily.vol` 为“手”口径；页面继续以成交额和估算净额为主，不展示成交量、规模或份额视图。
- [x] 评估是否需要把分类维护做成本地辅助报告，而不是放进分享页面；当前已从分享页移除，继续用本地 CSV 和校验输出维护。
- [x] 新增 ETF 生命周期事件表和本地份额跳变核对工具，用公告关键词识别拆分、折算、转型、清盘等事件。
- [x] 新增独立 `ETF公告更新.bat`，真实公告远端更新不接入日常启动 helper。
- [x] 将生命周期份额跳变阈值放入 `config.txt`，并将 Tushare 公告权限错误改为立即跳过远端更新。
- [x] 接入上交所/深交所官网基金公告/ETF公告源，作为公告下载主源，Tushare 仅保留为备用。
- [x] 将生命周期公告抓取窗口改为 `config.txt` 中的前后 5 个官方交易日。
- [x] 增加未抓到公告的待人工确认表、人工确认白名单和 BAT 启动交互确认。
- [x] 加固用户维护 CSV 的 Excel 兼容读写，覆盖生命周期 CSV 和 `etf_category_map.csv` 中文分类。
- [x] 用本地审计跳变小样本抓取交易所公告，验证“基金份额拆分结果”可回填生命周期表并从待抓清单中过滤。
- [x] 全量画像当前 871 条跳变，评估公告抓取范围应缩窄为高疑似生命周期层。
- [x] 将生命周期公告请求清单改成分层输出：高疑似自动抓取，低疑似仅保留本地观察报告。
- [x] 收敛 `ETF公告更新.bat` 控制台输出，默认只显示关键数量、报告路径和必要提醒；详细 JSON 改为写入摘要文件，必要时用 `--verbose` 打印。
- [x] 放慢交易所公告请求并增加连接断开重试；部分抓取失败时保留已抓结果、继续审计，并在 BAT 结束前汇总已抓/已审计/待抓数量。
- [x] 将公告下载主源切换为巨潮资讯，统一覆盖上交所/深交所 ETF 公告，交易所官网源保留备用。
- [x] 测试巨潮公告请求间隔，将正式默认从 1.0 秒调为配置项 `announcement_sleep_seconds = 0.20`。
- [x] 正式运行巨潮公告更新，完成高疑似跳变复查并将生命周期状态核对至 `20260626`。
- [x] 将无公告跳变从黑窗口人工确认改为前后 10 个官方交易日二轮自动复查；二轮仍无严格生命周期事件时自动确认无事件。
- [x] 对“可能触发基金合同终止情形”改为自动向后探测，只在命中严格清盘关键词后写入生命周期事件。
- [x] 生成并接入 `etf_lifecycle_flow_adjustments.csv`，日常资金流页面对拆分/折算/转型/清盘等非资金流份额跳变归零。
- [x] 调整 dashboard `走势` 页为左侧收窄图表、右侧固定区间流向列，静态展示 8 个区间的净流入/净流出金额；右侧外框与左图外框对齐，纵轴移到左侧，并统一标题注释样式。
- [x] 完成 P2 生成速度评估：区间批量生成复用一次历史横截面读取，缓存读取改为按需字段，区间模式默认瘦身 CSV 明细输出。
- [x] `资金流监控.bat` 增加模式选择：回车默认单日报表，模式 2 生成起止日期内所有交易日报表并输出心跳进度。
- [x] `tools/publish_pages.py` 支持一次性发布区间内已生成 dashboard。
- [x] 修复 ETF 公告更新在缓存起点前移后仍按旧 `verified_through` 误跳过的问题；状态现在同时校验 `verified_from` 和 `verified_through`。
- [x] 跑完前移缓存后新增的 70 条 2023 年高疑似生命周期公告复查；最终 request plan 归零，状态核对区间为 `20221230` 至 `20260626`。

## P1

- [ ] 人工复核 `data/local_reference/etf_category_map.csv`，尤其是 `宽基` 中的主题 ETF。
- [ ] 优化 dashboard 视觉细节和真实数据下的长表/极值显示。

## P2

- [ ] 未来版本评估桌面壳：历史区间查询、多时段切换、日报归档浏览。
