# 下一步工作 list 2026-06-26

## 已完成

- [x] 将 `write_dashboard_html` 接入 CLI，生成 `etf_flow_dashboard.html`。
- [x] 在 `资金流监控.bat` 中增加日期输入，留空时沿用 CLI 默认日期逻辑。
- [x] 将 HTML dashboard 路径写入 `run.json` outputs。
- [x] 运行 dry-run，确认空数据也能生成自包含 HTML。
- [x] 更新 README，说明单日静态 HTML 的使用和分享方式。
- [x] 增加 dashboard renderer 的基础测试，覆盖自包含输出、双视图、无外部资源引用。
- [x] 运行现有单元测试，确认资金流指标未被可视化改动破坏。
- [x] 运行 `tools/build_etf_category_map.py`，生成可编辑 ETF 分类映射文件 `data/local_reference/etf_category_map.csv`。
- [x] 从 Tushare `fund_basic` 建立 `data/cache/tushare/static/etf_basic_E.csv` 缓存。

## P0

- [x] 将 `data/local_reference/etf_category_map.csv` 接入 dashboard/flow snapshot，优先使用手工 `category/subcategory/review_note`。
- [x] 补官方交易日历逻辑：留空日期解析最近可用市场日，输入非交易日时明确映射或报错，并写入 `run.json`。
- [x] 新增 Tushare 数据源体检脚本，抽样验证 `fund_basic`、`fund_daily`、`fund_share` 可用性和权限/频率限制。

## P1

- [ ] 人工复核 `data/local_reference/etf_category_map.csv`，尤其是 `宽基` 中的主题 ETF。
- [ ] 复核完整真实跑数中 14 只缺失 `fund_daily` 的 ETF，判断是新上市无行情、停牌、代码问题还是接口缺失。
- [x] 新增 `tools/validate_category_map.py`，检查空分类、重复 `fund_code`、非预期分类名和待确认记录。
- [x] 给 dashboard 增加“未分类/待人工确认”检查区，方便维护分类表。
- [x] 继续增强 `run.json` 数据质量统计：ETF 基础信息数量、行情数量、份额数量、缺失比例、分类命中率。
- [x] 用真实 Tushare 数据跑一个完整监控日，验证 CSV/Markdown/HTML 输出。
- [x] 新增 `tools/probe_tushare_fetch_shapes.py`，确认 `fund_daily/fund_share` 支持按 `trade_date` 批量取数。
- [x] 将 CLI 优先切到按官方交易日横截面批量拉取，并缓存到 `daily_cross_section`。
- [x] 确认 `fund_share.fd_share` 第一层单位按“万份”处理，并修正 `estimated_net_flow = share_change × 10000 × close`。
- [x] 确认 `fund_share.trade_date=T` 对 A 股、港股通、QDII 样本均按 T 日份额处理。
- [x] 将 `港股海外` 分类拆成 `港股` 和 `海外`，并迁移当前分类表。
- [x] 对 `港股` / `海外` 类 ETF 缺失 A 股交易日数据时启用前一交易日数据填充。
- [x] 将数据更新 helper 挂到 BAT；更新失败时披露本地完整缓存区间，并用 `--cache-only` 尝试本地生成或跳过。
- [x] 新增 `tools/refine_category_map_with_sw.py`，缓存 Tushare SW2021 申万行业指数并细化 `etf_category_map.csv`。
- [x] 修正申万行业匹配的“综合”泛词误分，避免上证综合/创业板综合/科创板综合被误归为申万综合。

## P2

- [ ] 后续验证 Tushare `fund_share/fd_share` 是否代表 ETF 总份额。
- [ ] 从 ETF 公告建立生命周期表，核对拆分、折算、转型、清盘等非资金流份额跳变。
- [x] 用新横截面取数路径重跑完整默认 lookback，记录冷缓存/热缓存耗时。
- [ ] 若 31 秒热缓存仍偏慢，评估聚合缓存、按需字段读取和明细输出瘦身。
- [ ] 在字段口径确认后，再接入 `份额（亿份）` 和 `规模（亿元）` 视图。
- [ ] 优化 dashboard 视觉细节和真实数据下的长表/极值显示。
- [ ] 未来版本评估桌面壳：历史区间查询、多时段切换、日报归档浏览。
