# 工作进度 2026-07-03

- 定位 `data/local_reference/etf_category_map.csv` 手工编辑后导致 `资金流监控.bat` 失败的原因：文件被保存为 GB18030/Windows ANSI，当前读取逻辑固定使用 UTF-8 BOM，触发 `UnicodeDecodeError`。
- 将当前 `data/local_reference/etf_category_map.csv` 从 GB18030/Windows ANSI 转回 UTF-8 BOM，仅变更文件编码，不重新推断或改写分类语义。
- 调整 `src/etf_flow_monitor/utils/io.py`：`read_user_csv()` 仍优先读取 UTF-8 BOM，遇到解码失败时兜底读取 GB18030，兼容 Excel 在中文 Windows 下另存的 CSV。
- 新增 `tests/test_category_map.py` 用例，覆盖 GB18030 编码的分类表读取。
- 验证：`tools/validate_category_map.py --config config.txt` 通过，`rows=1582`、`blocking_issues=0`、`pending_review=0`，输出 `outputs/category_map_validation/category_map_validation_20260703_205706.json`。
- 验证：`python -X utf8 -c "import sys; sys.path.insert(0, 'src'); import pytest; raise SystemExit(pytest.main(['tests']))"` 通过，59 个测试全部通过。
- 验证：`etf_flow_monitor.cli --config config.txt --cache-only --dry-run` 可正常生成 dry-run 报告，输出 `outputs/flow_monitor/20260703/etf_flow_report.md` 和 `outputs/flow_monitor/20260703/etf_flow_dashboard.html`。
- 本次未变更远端数据源、缓存 schema 或资金流估算公式。