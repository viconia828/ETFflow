# 工作进度 2026-09-22

## GitHub Pages 部署确认超时排查与修复

- 用户日志为 2026-09-21 日报发布后等待部署确认 120 秒超时。旧脚本仅在 `git push` 成功后进入该检查，因此不能据此判断上传失败。
- 本地报表运行账本 `outputs/logs/flow_monitor_20260922_000340/run.json` 为 `success`，00:04:43（上海时间）完成。
- 现场访问 `https://viconia828.github.io/ETFflow/reports/20260921/?v=2f49d03e9e12` 返回 HTTP 200；线上 145,705 字节，本地 147,280 字节，统一 CRLF/LF 后内容完全一致。Git 配置 `core.autocrlf=true`，不能直接把原始字节散列不同认定为旧页面。
- 同时对 GitHub 分支、部署和 Actions 的 API 请求均报 `[Errno 11001] getaddrinfo failed`，PowerShell DNS 查询也超时。这证明当前部署查询接口的 DNS 存在故障；旧日志没有保留最后一次错误，无法严格追溯最初 120 秒内的每次失败原因。
- 排查证据及原始线上 HTML 保存于 `outputs/logs/pages_diagnostic_20260922/`；`run.json` 保留请求结果、散列、换行归一化比较和修复后线上核验结果。

## 修改

- `tools/publish_pages.py`：上传成功输出提交 SHA；无文件变化时也检查现有部署，避免重试后直接误报成功。
- 单日报告查询异常或超时增加线上内容核验，只归一化 CRLF/LF；旧页面、内容不匹配及网络错误均不认定成功。API 明确返回失败时仍报失败，多日区间保持 API 确认。
- 保留超时前最后一次状态、错误说明与部署日志/Actions 链接；未确认返回 `2`，明确失败返回 `1`，成功或显式跳过返回 `0`。
- 默认轮询间隔从 3 秒调整到 10 秒，降低未认证 API 请求频率；等待时长仍为 120 秒。
- `资金流监控.bat` 区分“文件已在 GitHub、部署未确认”和明确发布失败；`README.md` 同步说明。
- 没有变更行情数据源、缓存 schema、指标公式、已有本地研究数据或系统 DNS 设置，也未重新推送远端。

## 验证

- 使用已有依赖运行时 `C:/Users/Ai/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe`，确认 pandas 3.0.1；未安装依赖或新建环境。
- 新增 `tests/test_publish_pages.py`，标准库 unittest 执行 10 个测试全部通过，覆盖 DNS 错误、旧页面、换行差异、明确失败优先、超时诊断保留、返回码、区间限制及无变更重查。
- 直接执行原发布相关的 11 个回归函数全部通过（已有运行时没有 pytest，临时路径使用标准库 tempfile 提供）。
- 新线上核验函数实测返回 `content_verified`，证明该日报已经上线；`git diff --check` 通过。

## 区间发布 HTTP 403 限流复查

- 用户随后发布 `20260914..20260921` 共 6 份报告，提交 `b00c1b725afb0e454c8c6d227e4bb9f0edbcf358` 上传成功。此次错误明确为 `HTTP Error 403: rate limit exceeded`，与上一轮现场 DNS 失败不同。
- 脚本使用未认证 REST API，按 GitHub 官方文档该额度按出口 IP 共享，每小时 60 次；降低轮询频率不能消除重复运行或共享出口导致的额度耗尽。此前的异常处理仍会在限流后继续请求，需停止这种重试。
- 未重复请求受限 API；逐一下载 20260914、20260915、20260916、20260917、20260918、20260921 的公开页面，全部 HTTP 200，统一换行后全部与当前本地报告一致。最新版本为 `853fcd7ce2b9`。
- 新证据与原始页面保存到 `outputs/logs/pages_range_diagnostic_20260922_003910/`，`run.json` 的 `all_match=true`。
- `tools/publish_pages.py` 增加明确限流识别（403 的额度头/错误说明，以及 429），识别后本次运行不再查询 API，保留服务端恢复时间/Retry-After；在剩余等待时间内通过公开页面核验进度。
- 将内容核验扩展到本次发布的每份日报，全部一致才确认成功；最多并发 3 个请求，一批中有旧页面或网络失败则停止本轮扫描。该改动替代上节中“多日区间只使用 API”的限制。
- 普通权限型 403 不误认作限流，API 明确部署失败仍优先返回失败；区间尚未全部匹配时保持未确认，不会仅检查最后一期。
- 17 个发布单元测试和原有 11 个发布回归函数全部通过；新多日报告核验函数现场返回 `All 6 dashboard(s) verified online`。
- 未重新生成报告、推送远端或修改 DNS；行情数据源、缓存 schema 和指标公式均未变。

## 全部本地变更汇总与版本提交

- 按用户要求，将本轮发布修复、测试、README、BAT、工作日志，以及工作区已有的全部公告/生命周期参考数据变更一并纳入提交，目标为 `origin/main`。
- 相对上一提交，公告表从 2,911 条增至 3,017 条，生命周期事件从 197 条增至 228 条，资金流调整记录从 112 条增至 130 条，人工确认从 514 条增至 562 条，观察记录从 492 条增至 505 条。
- 公告请求从 38 条降至 1 条，待确认记录从 2 条降至 1 条。剩余为 `159328.SZ`（易方达中证家电龙头 ETF）2026-09-18 份额跳变，状态 `no_announcement_found`；请求窗口延伸至 2026-09-28，不将“暂未找到公告”当作已确认无生命周期事件。
- 生命周期状态记录的数据末日为 2026-09-21，核验截至 2026-09-15，`lifecycle_current=false`，保留未完成状态。本轮提交仅保存现有参考数据，不重新抓取或改写其结论。
- 数据源接口、缓存 schema、资金流计算公式没有新增变更；既有调整记录的增量随参考数据一起保存。
- 已通过 17 个发布单元测试与 11 个原有发布回归检查，并完成 6 份日报线上内容核验；提交前 `git diff --check` 通过。
- 缓存、输出报告、诊断原始证据及本机配置继续按现有 `.gitignore` 保存在本地，不强制纳入版本库。
