# 工作进度 2026-06-28

- 确认 `资金流监控.bat` 在单日报表空日期时使用官方交易日历解析默认报告日：非交易日映射到前一个交易日，交易日也回退到前一个交易日，避免默认生成尚未完整落地的当天数据。
- 调整手动输入日期的当天边界：当 `--trade-date` / BAT 输入的是当前自然日时，即使当天是交易日，也按“最近可用市场日”回退到前一个交易日；历史交易日仍按输入日期生成。缓存更新、页面生成和 Pages 发布三步均使用该口径，避免发布残留的当天 dashboard。
- 调整 dashboard 明细页分类列：由“默认显示主分类、鼠标悬停显示 `category / subcategory`”改回直接显示 `category / subcategory`；当主分类和子分类相同时只显示一次，兼容手机端无 hover 的场景。
- 同步更新 `README.md` 和 HTML dashboard 测试断言；未改动数据源、缓存 schema 或资金流估算公式。
